1. Run /check — must be green.
2. `git status` — confirm js/config.js and .env are not staged; grep staged diff for `service_role` and `eyJ`.
3. Commit with a conventional message and push to main (Pages/Vercel auto-deploys).
4. Report the deployed URL and what changed.
